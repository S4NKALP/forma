[project]
name = "mine"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fabric",
    "pillow>=12.3.0",
    "tomlkit>=0.15.1",
]

[tool.uv.sources]
fabric = { git = "https://github.com/Fabric-Development/fabric.git" }

[dependency-groups]
dev = [
    "ruff>=0.16.7",
]
